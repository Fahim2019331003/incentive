//ekhan theke kaj shuru korbo

interface formInput {
  email?: string;
  designation;
}

export default async function checkApplication(formInput) {
  try {
  } catch (error: any) {
    return error.mesage;
  }
}
