'use client';

import { getKeyValue,TableCell } from "@nextui-org/react";

const Tableitem = ({item,columnKey}) => {
    // console.log(item);
    // console.log(columnKey);
  return (
    
    // <TableCell>{getKeyValue(item, columnKey)}</TableCell>
     <TableCell>Hi</TableCell>
    
  );
};

export default Tableitem;
